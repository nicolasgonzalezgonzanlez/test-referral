/**
 * Required servides
 */

const addressService = require('./address.service');
const addressServiceMock = require('./mock/address.mock');

/**
 * Export Services
 */

module.exports = {
    ...addressService,
    mock: {
        ...addressServiceMock
    }
}